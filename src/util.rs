/*use ggez::nalgebra as na;

pub struct Util {}

impl Util {
    pub fn rotate(v: na::Vector2<f32>, a: f32) -> na::Vector2<f32> {
        na::Vector2::new(v.x * a.cos() - v.y * a.sin(), v.x * a.sin() + v.y * a.cos())
    }
}*/